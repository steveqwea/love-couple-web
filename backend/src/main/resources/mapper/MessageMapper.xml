<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.love.mapper.MessageMapper">

    <insert id="insert" parameterType="com.love.entity.Message" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO message (couple_code, sender_name, content, create_time)
        VALUES (#{coupleCode}, #{senderName}, #{content}, NOW())
    </insert>

    <select id="listByCoupleCode" resultType="com.love.entity.Message">
        SELECT * FROM message WHERE couple_code = #{coupleCode} ORDER BY create_time DESC
    </select>

    <delete id="deleteById">
        DELETE FROM message WHERE id = #{id}
    </delete>

</mapper>
