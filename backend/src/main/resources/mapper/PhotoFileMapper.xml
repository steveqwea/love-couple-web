<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.love.mapper.PhotoFileMapper">

    <insert id="insert" parameterType="com.love.entity.PhotoFile" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO photo_file (url, content_type, data, create_time)
        VALUES (#{url}, #{contentType}, #{data}, NOW())
    </insert>

    <select id="selectByUrl" resultType="com.love.entity.PhotoFile">
        SELECT id, url, content_type AS contentType, data, create_time AS createTime
        FROM photo_file WHERE url = #{url}
    </select>

    <delete id="deleteByUrl">
        DELETE FROM photo_file WHERE url = #{url}
    </delete>

</mapper>
